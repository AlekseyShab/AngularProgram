import { BreadcrumbUrlPipe } from './breadcrumb-url.pipe';

describe('BreadcrumbUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BreadcrumbUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
